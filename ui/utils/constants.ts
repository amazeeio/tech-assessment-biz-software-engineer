// this would be even better to store in env variables, but due to inclusion of these variables in the gitignore file
// it wouldn't be visible on git for the sake of demonstration
export const SERVER_BASE_URL = "http://localhost:3000/graphql";
export const CLIENT_BASE_URL = "http://localhost:3001";
